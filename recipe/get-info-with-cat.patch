diff --color=auto -ur a/lib/git-subrepo b/lib/git-subrepo
--- a/lib/git-subrepo	2024-07-25 18:52:20
+++ b/lib/git-subrepo	2026-02-03 08:47:59
@@ -1734,8 +1734,8 @@
     # If `git-subrepo` was system-installed (`make install`):
     if [[ -e $lib/git-subrepo.d/upstream ]] &&
        [[ -e $lib/git-subrepo.d/commit ]]; then
-      command_remote=$(readlink "$lib/git-subrepo.d/upstream")
-      command_commit=$(readlink "$lib/git-subrepo.d/commit")
+      command_remote=$(cat "$lib/git-subrepo.d/upstream")
+      command_commit=$(cat "$lib/git-subrepo.d/commit")
     elif [[ $lib =~ / ]]; then
       lib=$(dirname "$lib")
       if [[ -d $lib/.git ]]; then
